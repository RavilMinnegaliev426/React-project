"use strict";exports.__esModule = true;exports.fixClickFocusIE = void 0;var _getFocusableElements = require("../dom/getFocusableElements");
var _utils = require("../utils");

var fixClickFocusIE = function fixClickFocusIE(e) {
  if (_utils.isIE11 || _utils.isEdge) {
    // workaround for the IE/Edge focus bug
    // https://developer.microsoft.com/en-us/microsoft-edge/platform/issues/14306303/
    if (document.activeElement !== e.target) {
      if (e.target instanceof HTMLElement) {
        var closestFocusable = (0, _getFocusableElements.getClosestFocusableElement)(e.target);
        if (closestFocusable) {
          closestFocusable.focus();
        }
      }
    }
  }
};exports.fixClickFocusIE = fixClickFocusIE;