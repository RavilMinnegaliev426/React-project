"use strict";exports.__esModule = true;exports.isValidPositions = void 0;var _currentEnvironment = require("../../lib/currentEnvironment");


var _PopupMenuPositions = require("./PopupMenuPositions");

var isValidPosition = function isValidPosition(position) {
  return _PopupMenuPositions.PopupMenuPositions.includes(position);
};

var isValidPositions = function isValidPositions(positions) {
  return positions.every(function (item) {
    if (_currentEnvironment.isProductionEnv) {
      return isValidPosition(item);
    } else {
      if (isValidPosition(item)) {
        return true;
      } else {
        throw new Error("Unxpected position \"" + item + "\"");
      }
    }
  });
};exports.isValidPositions = isValidPositions;