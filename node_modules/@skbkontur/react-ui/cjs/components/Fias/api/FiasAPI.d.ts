import { FiasAPIProvider, FiasAPIResult, FiasFetchFn, FiasCountry, FiasSearchOptions, FiasSearchResponse, FiasVerifyResponse } from '../types';
import { FiasAddress } from '../models/FiasAddress';
export declare class FiasAPI implements FiasAPIProvider {
    private baseUrl;
    private version?;
    private fetchFn;
    private static searchStopWords;
    private static createQuery;
    private static trimSearchText;
    private regionsPromise;
    constructor(baseUrl?: string, version?: string | undefined, fetchFn?: FiasFetchFn);
    verify: (address: FiasAddress) => Promise<FiasAPIResult<FiasVerifyResponse>>;
    search: ({ fiasId, searchText, field, parentFiasId, limit, fullAddress, directParent, }: FiasSearchOptions) => Promise<FiasAPIResult<FiasSearchResponse>>;
    searchCountry: (query: {
        prefix: string;
        limit?: number | undefined;
    }) => Promise<FiasAPIResult<FiasCountry[]>>;
    private send;
    private resolveFiasId;
    private searchAddressObject;
    private resolveAddress;
    private searchStead;
    private searchHouse;
    private searchRoom;
    private searchRegions;
}
