import { FiasAPIProvider, FiasSearchOptions, FiasSearchResponse, FiasVerifyResponse, FiasAPIResult, FiasCountry } from '../types';
import { FiasAddress } from '../models/FiasAddress';
export declare class FiasMockAPI implements FiasAPIProvider {
    verify: (address: FiasAddress) => Promise<FiasAPIResult<FiasVerifyResponse>>;
    search: ({ fiasId, searchText, field, parentFiasId, limit, fullAddress, }: FiasSearchOptions) => Promise<FiasAPIResult<FiasSearchResponse>>;
    searchCountry: (options: FiasSearchOptions) => Promise<FiasAPIResult<FiasCountry[]>>;
    private getAddresses;
}
