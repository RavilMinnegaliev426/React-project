import React from 'react';
export interface PreventProps {
    children?: React.ReactNode;
}
export declare class Prevent extends React.Component<PreventProps> {
    render(): JSX.Element;
    private _prevent;
}
