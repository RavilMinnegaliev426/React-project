"use strict";exports.__esModule = true;exports.ProductWidget = void 0;var calledLoad = false;

var ProductWidget = {
  init: function init() {
    var loadWidget = function loadWidget() {
      if (calledLoad) {
        return;
      }
      calledLoad = true;

      var script = document.createElement('script');
      script.src = 'https://widget-product.kontur.ru/widget/loader?' + 'product=&type=service';
      setTimeout(function () {return document.getElementsByTagName('head')[0].appendChild(script);});
    };

    loadWidget();
  } };exports.ProductWidget = ProductWidget;