import React from 'react';
import PropTypes from 'prop-types';
export interface TopBarUserProps {
    userName: string;
    cabinetUrl?: string;
}
/**
 * Меню работы с кабинетом пользователя
 *
 * @visibleName TopBar.User
 */
export declare class TopBarUser extends React.Component<TopBarUserProps> {
    static __KONTUR_REACT_UI__: string;
    static defaultProps: {
        cabinetUrl: string;
    };
    static propTypes: {
        userName: PropTypes.Requireable<string>;
        /**
         * URL для кастомизации ссылок в меню пользователя
         */
        cabinetUrl: PropTypes.Requireable<string>;
    };
    private readonly locale;
    render(): JSX.Element;
}
