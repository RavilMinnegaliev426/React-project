import React from 'react';
import { Gapped } from "../../../components/Gapped";
import { jsStyles } from "../Playground.styles";
export var ComponentsGroup = function ComponentsGroup(props) {
  var title = props.title,
      children = props.children,
      style = props.style,
      theme = props.theme;
  return React.createElement(Gapped, {
    wrap: true,
    verticalAlign: "top",
    gap: 40
  }, React.createElement("div", {
    className: jsStyles.title(theme),
    style: style
  }, title), React.createElement("div", {
    className: jsStyles.componentsGroup(theme)
  }, React.createElement(Gapped, {
    wrap: true,
    verticalAlign: "middle",
    gap: 10
  }, children)));
};