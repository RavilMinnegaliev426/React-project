import * as PropTypes from 'prop-types';
export function safePropTypesInstanceOf(getExpectedClass) {
  if (typeof window !== 'undefined') {
    return PropTypes.instanceOf(getExpectedClass());
  }

  return PropTypes.any;
}