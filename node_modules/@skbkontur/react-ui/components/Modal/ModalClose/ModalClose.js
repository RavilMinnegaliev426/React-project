import React, { useContext } from 'react';
import cn from 'classnames';
import { ThemeContext } from "../../../lib/theming/ThemeContext";
import { jsStyles } from "../Modal.styles";
export function ModalClose(_ref) {
  var _cn;

  var disableClose = _ref.disableClose,
      requestClose = _ref.requestClose;
  var theme = useContext(ThemeContext);
  return React.createElement("button", {
    className: cn((_cn = {}, _cn[jsStyles.close(theme)] = true, _cn[jsStyles.disabled(theme)] = disableClose, _cn)),
    onClick: requestClose,
    "data-tid": "modal-close"
  }, React.createElement("span", {
    className: jsStyles.closeOutline(theme)
  }));
}