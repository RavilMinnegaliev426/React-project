import React, { useContext } from 'react';
import cn from 'classnames';
import { ThemeContext } from "../../../lib/theming/ThemeContext";
import { jsStyles } from "../MenuHeader.styles";
/**
                                                 * Заголовок в меню.
                                                 */

function MenuHeader(_ref) {
  var _cn;

  var _ref$_enableIconPaddi = _ref._enableIconPadding,
      _enableIconPadding = _ref$_enableIconPaddi === void 0 ? false : _ref$_enableIconPaddi,
      children = _ref.children;

  var theme = useContext(ThemeContext);
  return React.createElement("div", {
    className: cn((_cn = {}, _cn[jsStyles.root()] = true, _cn[jsStyles.withLeftPadding(theme)] = _enableIconPadding, _cn))
  }, children);
}

MenuHeader.__KONTUR_REACT_UI__ = 'MenuHeader';
MenuHeader.__MENU_HEADER__ = true;
export { MenuHeader };
export var isMenuHeader = function isMenuHeader(child) {
  return React.isValidElement(child) ? Object.prototype.hasOwnProperty.call(child.type, '__MENU_HEADER__') : false;
};