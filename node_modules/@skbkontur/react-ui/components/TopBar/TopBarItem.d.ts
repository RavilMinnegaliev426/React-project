import React from 'react';
import PropTypes from 'prop-types';
import { IconProps } from '../../internal/icons/20px';
export interface TopBarItemProps {
    _onClick?: (event: React.MouseEvent<HTMLDivElement>) => void;
    _onKeyDown?: (event: React.KeyboardEvent<HTMLDivElement>) => void;
    active?: boolean;
    children?: React.ReactNode;
    className?: string;
    icon?: IconProps['name'];
    iconOnly?: boolean;
    minWidth?: string | number;
    use: 'danger' | 'pay' | 'default';
    tabIndex?: number;
}
/**
 * Статичный элемент топбара
 *
 * @visibleName TopBar.ItemStatic
 */
export declare class TopBarItem extends React.Component<TopBarItemProps> {
    static __KONTUR_REACT_UI__: string;
    static propTypes: {
        use: PropTypes.Requireable<string>;
    };
    static defaultProps: {
        className: string;
        use: string;
    };
    private getProps;
    render(): JSX.Element;
}
