import React from 'react';
import { IconProps } from '../../internal/icons/20px';
import { DropdownMenuProps } from '../DropdownMenu';
export interface TopBarDropdownProps extends Omit<DropdownMenuProps, 'caption' | 'disableAnimations'> {
    icon?: IconProps['name'];
    minWidth?: string | number | null;
    use: 'danger' | 'pay' | 'default';
    label: React.ReactNode;
    onOpen?: () => void;
    onClose?: () => void;
}
/**
 * Дропдаун в топбаре
 *
 * @visibleName TopBar.Dropdown
 */
export declare class TopBarDropdown extends React.Component<TopBarDropdownProps> {
    static __KONTUR_REACT_UI__: string;
    static defaultProps: {
        use: string;
    };
    private dropdownMenu;
    render(): JSX.Element;
    open: () => void;
    close: () => void;
    private refDropdownMenu;
    private renderButton;
    private handleKeyDown;
}
