import { FiasAPIResult } from '../types';
export declare class FiasAPIResultFactory {
    static success: <Data>(data: Data) => FiasAPIResult<Data>;
    static fail: <Data>(message?: string) => FiasAPIResult<Data>;
}
